// DOM : document object model
// complete Html tree Object :document
// browser will add this document object into window object of browser itself
// browser is able to display webpage based on document object
console.log(window.document) // this is will whole html page in console
console.dir(window.document) // JS perspective : complete object with properties

// select document element using getElementById()

console.log(document.getElementById('main-heading')) //returns element object
